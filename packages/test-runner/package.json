{
  "name": "@fluxiq-web-extension/test-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "domain:dist": "node -e \"const fs=require('node:fs');if(fs.existsSync('../../domain/dist/index.d.ts'))process.exit(0);console.log('[test-runner] domain/dist is absent: building @fluxiq-web-extension/domain once so this package can resolve its ./node declarations. An existing dist is never rebuilt here.');process.exit(1)\" || pnpm --filter @fluxiq-web-extension/domain build",
    "build": "pnpm domain:dist && tsc -p tsconfig.json",
    "check": "pnpm domain:dist && tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@fluxiq-web-extension/domain": "workspace:*",
    "@fluxiq-web-extension/test-contracts": "workspace:*",
    "@fluxiq-web-extension/test-evidence": "workspace:*",
    "@playwright/test": "1.51.1",
    "fluxiq": "link:../../../!FluxIQ/packages/fluxiq"
  },
  "bin": {
    "fluxiq-lab": "./dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}

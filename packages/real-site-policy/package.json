{
  "name": "@fluxiq-web-extension/real-site-policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "fluxiq-real-site-policy": "./dist/cli.js" },
  "exports": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}

import { type ExtensionStatus, type FluxIQSession, type FluxIQSettings, type RecordingEventPayload } from "../shared/protocol";
type StatusListener = (status: ExtensionStatus) => void;
export declare class FluxIQConnection {
    private settings;
    private session;
    private socket;
    private heartbeatTimer;
    private reconnectTimer;
    private reconnectAttempt;
    private connectionState;
    private recordingState;
    private lastError;
    private lastMessageAt;
    private activeTabId;
    private activeTabUrl;
    private pairingReferenceCode;
    private queueSize;
    private shouldStayConnected;
    private readonly listeners;
    constructor(settings: FluxIQSettings, session: FluxIQSession);
    status(): ExtensionStatus;
    subscribe(listener: StatusListener): () => void;
    updateSettings(settings: FluxIQSettings): void;
    connect(): Promise<void>;
    disconnect(): void;
    startRecording(): Promise<void>;
    stopRecording(): Promise<void>;
    handleRecordingEvent(payload: RecordingEventPayload, tabId?: number, frameId?: number): Promise<void>;
    handleTabUpdated(tab: chrome.tabs.Tab): Promise<void>;
    private onOpen;
    private onClose;
    private onError;
    private onMessage;
    private handleServerCommandPayload;
    private sendHello;
    private sendBrowserState;
    private sendActionResult;
    private sendClientMessage;
    private flushQueue;
    private startHeartbeat;
    private stopHeartbeat;
    private scheduleReconnect;
    private clearReconnect;
    private refreshActiveTab;
    private broadcastToContent;
    private setState;
    private emitStatus;
}
export {};

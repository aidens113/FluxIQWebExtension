# Module Size Governance Plan

Status: Active
Status detail: Enforcement live and mirrored from Core; Phase 1 tests relocation is complete; the file splits are not started.
Created: 2026-09-10
Last updated: 2026-09-10
Owner: Senior supervisor agent
Scope: Applying the shared file and class size policy to this repository, and decomposing the files here that already exceed it.
Paired document: `F:\!FluxIQ\docs\working\module-size-governance-plan.md`
Related: [AGENTS.md](../../AGENTS.md), [agent working document protocol](./agent-working-doc-protocol.md)

The shared policy — thresholds, ratchet semantics, rationale, and the
CodeGraph assessment — is owned by the paired Core document. This document
covers only this repository's offenders and wiring, and does not restate the
policy.

---

## Current State

**Phase 1 is complete here.** All 74 co-located tests now sit in a `tests/`
subfolder of the directory that owns their subject. The `test-placement` rule
reports zero findings, down from 13 directories holding 74 files. Two workers
did the move, partitioned by package: `packages/test-runner` (51) and
everything else (23).

**Enforcement is live here.** `scripts/structure-audit.mjs` and its
`scripts/structure-audit/` tree are mirrored from Core byte for byte — change
them there first — with this repository's own `config.mjs`, which
additionally forbids `domain/src` importing `apps/extension/src`, and its own
`.structure-baseline.json`. `pnpm check` now runs `pnpm structure:test`
first, then the audit, then `pnpm -r check`, so the rules are validated
before they judge the repository. `docs/working/README.md` is generated by
the audit.

**The `imports` rule changed during Phase 1, and the change matters.**
Relocating a test into `tests/` turns every same-directory import (`./x`,
explicitly exempt) into `../x`, which the rule counted as reaching past the
directory's barrel. Left alone, Phase 1 would have raised this repository's
frozen barrel-skip count from 29 to roughly 80, and Core's by over 130 —
defeating the ratchet the plan exists to install. The rule now also exempts a
test under `<dir>/tests/` importing `<dir>`'s own modules: such a test is not
a consumer crossing a boundary, and the only alternatives were widening
public barrels purely so tests could reach their subjects, or freezing a
number that Phase 1 itself inflated.
`scripts/structure-audit/rules/tests/imports.test.mjs` pins the behaviour
with three widening cases and four skips the rule must still catch.

**Audit, this repository:** 4 files over 800 lines; 3 directories over 25
files (`packages/test-runner/src` 50, `packages/test-runner/src/tests` 51,
`scripts/` 32); 1 class over 40 methods (`FluxIQConnection` in
`apps/extension/src/background/connection.ts`, 81); 2 files over 15 exported
values; **0 co-located tests**, down from 74; 5 shared-prefix groups; 26
barrel-skipping imports across 22 files, down from 29 across 23; zero
domain→extension boundary violations. The baseline shrank from 53 keys to 40
and no entry rose.

| File | Lines | Note |
| --- | --- | --- |
| `packages/test-runner/src/demo-workspace.ts` | 3,855 | Test infrastructure, not shipped product code. |
| `apps/extension/src/background/connection.ts` | 1,913 | Shipped. 81 methods on `FluxIQConnection`: WebSocket session, tab routing, recording state, storage. |
| `apps/extension/src/content/index.ts` | 1,188 | Shipped. Content script entry point. |
| `packages/test-runner/src/demo-llm-create-ui.ts` | 837 | Test infrastructure. |

**Next steps**

1. Split the oversized files by their pathology — `connection.ts` first,
   since it ships and is also an 81-method class; then `content/index.ts`.
   Sequence per Core's plan. Run `pnpm structure:baseline` after each.
2. `packages/test-runner/src/tests` (51 files) is over the directory limit as
   a direct consequence of Phase 1: it inherited the tests of an oversized
   parent. It is baselined and may only shrink. Splitting `test-runner/src`
   by its `demo-` prefix group should carry the matching tests with it rather
   than being treated as a separate problem.
3. Before splitting `demo-workspace.ts`, note that
   `packages/test-runner/src/tests/demo-llm-prepare.test.ts` reads it and
   `demo-llm-blank-workspace.ts` as **text**. That split will break the test
   at runtime with no compile error.

**Known pre-existing defects, surfaced by Phase 1 but not caused by it.**
None were fixed, to keep a mechanical phase mechanical.

- `packages/test-runner/src/tests/demo-llm-creation.test.ts` asserts
  `maxCallsPerRun: 1` against a config that now returns `2` plus
  `maxInputTokens`/`maxOutputTokens`. A genuine stale expectation.
- 18 further `test-runner` failures are a cwd artifact, not defects: the
  tests pass `process.cwd()` as `repositoryRoot`, and `pnpm --filter` sets
  cwd to the package directory. Run from the repository root the same 266
  tests give 265 pass / 1 fail. Root `pnpm test` has therefore been red in
  this package since before Phase 1.
- `domain/src/runtime/tests/*.test.ts` (3 files) are compiled and run by
  nothing, before and after the move.

The methodology — ownership / layer / feature / kind placement, the
prefix-becomes-directory rule, one exported thing per file, barrels,
per-directory `tests/` subfolders, and the six division pathologies — is
authored in Core at `docs/architecture/code-structure.md` and applies here
without modification.

**Blockers:** none.
---

## Local Notes

**`connection.ts` is the one to watch.** At 1,913 lines it owns, per the
architecture description in `AGENTS.md`, the WebSocket session, tab routing,
recording state, and storage. That is four responsibilities, and the
repository's own modularity rule asks that they be separable. It is also
directly in the path of MVP Week 4's reliability hardening work — extension
reconnect, runtime reconnect, tab closure, network failure. If that hardening
requires substantial edits inside it, splitting the reconnection and session
lifecycle out at that point is justified on its own terms rather than as a
refactor for its own sake.

**`demo-workspace.ts` should not be exempt merely for being test code**, but
its 3,855 lines are the least urgent problem in either repository. Baseline
it and let the ratchet stop it growing.

**Existing tooling to reuse.** This repository already has
`packages/boundary-audit` with a `pnpm boundary:audit` entry point. If a
size audit becomes a second standalone script, that is two audit tools with
separate invocations. Worth considering whether the size check belongs inside
the existing audit package instead of alongside it.

---

## Worker Briefs

### Phase 1 dispatch — 2026-09-10

Core's Migration Plan Phase 1, applied here. Mechanical; no behaviour change.

**Shared context.** In every directory you own that holds test files
(`*.test.ts`, `*.test.mjs`, `*.spec.ts`), create a `tests/` subdirectory and
`git mv` the test files into it with filenames unchanged. Then repair
relative imports inside the moved tests — one extra `../` to reach the
subject, or import from the directory barrel. Move only test files:
fixtures, factories, mocks, and other support stay where they are. Change
nothing inside a test but its import specifiers. Never edit a subject
module to accommodate a test.

**The depth hazard is the real risk here.** Several packages run
`pnpm build && node --test dist/**/*.test.js`, and `**` in `sh` does not
recurse — it behaves as a single `*`. Moving `src/a.test.ts` to
`src/tests/a.test.ts` changes the compiled path from `dist/a.test.js` to
`dist/tests/a.test.js`, which can silently stop matching. A suite that no
longer runs is indistinguishable from a suite that passes. So: record the
number of test files and test cases that run **before** your move, and
again after. They must match. If a glob no longer matches, fix the glob in
that package's `package.json` and say so in your report.

Verify with `node scripts/structure-audit.mjs --rule test-placement --json`:
every directory you own must appear in `lowerable` or disappear entirely,
and `failures` must stay empty. Do **not** run `pnpm structure:baseline` —
the supervisor regenerates the baseline once, after both workers land.

### Brief: ext-test-runner
- Repository: this repository
- Task: Phase 1 relocation for `packages/test-runner` — 51 test files sitting directly in `src`, the single densest directory in this repository (101 source files). Move them to `src/tests/`. This package's `test` script is `pnpm build && node --test dist/**/*.test.js`; confirm the glob still matches at the new depth and correct it if not. Leave `demo-*.ts` and every other non-test module exactly where it is — the `demo-` prefix group and the 3,855-line `demo-workspace.ts` are later phases, not yours.
- Required reads: `Current State` of `docs/working/module-size-governance-plan.md`; the shared context above
- Owns (may edit): `packages/test-runner/**`
- Must not touch: every other package, `.structure-baseline.json`, root `package.json`
- Definition of done: files moved, imports repaired; `pnpm --filter @fluxiq-web-extension/test-runner test` observed passing with the same test-file and test-case counts as before the move, both quoted in your report; audit shows `packages/test-runner/src` in `lowerable` with `failures` empty; report written
- Report to: docs/working/module-size-governance-plan/reports/ext-test-runner.md

### Brief: ext-rest
- Repository: this repository
- Task: Phase 1 relocation everywhere else — 23 test files. Three sub-tasks, each with its own wrinkle.
  1. **`src` relocations**, the ordinary case: `apps/scenario-lab/src` (3) plus its `scenarios/instruction-only-form` and `scenarios/llm-target-drift` (1 each); `domain/src` (1) and `domain/src/runtime` (3); `packages/boundary-audit/src`, `packages/real-site-policy/src`, `packages/test-matrix/src` (1 each). Also change `domain/tsconfig.json` `exclude` from `["src/**/*.test.ts", ...]` to exclude `src/**/tests/**` instead of the filename pattern, keeping the other two entries. Check `domain/scripts/test-domain.mjs` for its own discovery pattern before assuming the move is transparent.
  2. **`test/` → `tests/` renames**: `packages/agent-orchestrator/test` (2), `packages/test-contracts/test` (4), `packages/test-evidence/test` (1). The audit accepts only `tests` or `e2e` as a test root directory name, so these fail purely on the singular name. `git mv` the directory and update each package's `test` script glob from `test/*.test.mjs` to `tests/*.test.mjs`.
  3. **`apps/extension/e2e/specs` (4 Playwright specs)**: the audit requires test files to sit *directly* inside a directory named `tests` or `e2e`, so `e2e/specs/` fails on nesting. Move the four `*.spec.ts` up into `e2e/` beside `playwright.config.ts` and `fixtures/`, then change that config's `testDir` from `"./specs"` to `"."`. Confirm `testMatch`/`testDir` still collects exactly four specs and does not start collecting anything under `fixtures/` or `test-results/`. Do not run the Playwright suite — report the collected spec list (`--list`) instead.
- Required reads: `Current State` of `docs/working/module-size-governance-plan.md`; the shared context above
- Owns (may edit): `apps/extension/e2e/**`, `apps/scenario-lab/**`, `domain/**`, `packages/agent-orchestrator/**`, `packages/boundary-audit/**`, `packages/real-site-policy/**`, `packages/test-contracts/**`, `packages/test-evidence/**`, `packages/test-matrix/**`
- Must not touch: `packages/test-runner/**`, `apps/extension/src/**`, `.structure-baseline.json`, root `package.json`
- Definition of done: all three sub-tasks done; each touched package's own `test` script observed passing with unchanged counts (quote them per package); `playwright test --list` in `apps/extension/e2e` shows the same four specs; audit shows all your directories in `lowerable` with `failures` empty; report written
- Report to: docs/working/module-size-governance-plan/reports/ext-rest.md

## Work Ledger

### 2026-09-10 — Plan authored

- Agent: supervisor
- Changed: this document and its Core pair.
- Why: Core reached a 12,482-line class while an instruction forbidding it
  was in force; this repository needs the same mechanical guard before it
  drifts the same way.
- Validation: `wc -l` over all tracked `.ts`, `.tsx`, `.mjs`, and `.js`
  files excluding `node_modules` and build outputs -> 287 source files, four
  over 800 lines, one over 2,000. Plan only, so no code check applies.
- Outcome: Accepted
- Follow-up: Implement the shared ratchet in Core, then wire it here.

### 2026-09-10 — Audit adopted and wired into pnpm check

- Agent: supervisor
- Changed: `scripts/structure-audit.mjs` and `scripts/structure-audit/`
  (mirrored from Core), `scripts/structure-audit/config.mjs` (this
  repository's boundaries), `.structure-baseline.json`, `package.json`
  (`check`, `structure:check`, `structure:baseline`), `AGENTS.md`,
  `docs/working/README.md` (now generated).
- Why: AGENTS.md stated budgets here that nothing enforced.
- Validation: `node scripts/structure-audit.mjs --rule imports --json` ->
  zero non-ratcheted findings, so the domain→extension boundary holds
  today; `pnpm structure:check` exit 0 after `--update`; `pnpm -r check`
  run alongside.
- Outcome: Accepted
- Follow-up: Phase 1 tests relocation.

---

### 2026-09-10 — Phase 1 tests relocation

- Agent: supervisor, with workers `ext-test-runner` and `ext-rest`
- Changed: 74 test files moved into `tests/` subfolders — 51 in
  `packages/test-runner/src`, 23 across scenario-lab, domain, and six
  packages. `packages/{agent-orchestrator,test-contracts,test-evidence}/test`
  renamed to `tests` with their `package.json` globs. Four Playwright specs
  lifted from `apps/extension/e2e/specs` into `e2e/` with `testDir` changed,
  because the rule requires tests directly inside a `tests` or `e2e`
  directory and widening this repository's `testRootDirNames` to accept
  `specs` would have diverged the one config file allowed to differ from
  Core. Also `domain/tsconfig.json`, `domain/scripts/test-domain.mjs`,
  `packages/test-runner/package.json` (test glob quoted),
  `scripts/structure-audit/rules/imports.mjs` plus its new test (both
  mirrored from Core), root `package.json` `check`, `.structure-baseline.json`.
- Why: Core's Migration Plan Phase 1. Halving the file count in dense
  directories before any code is split, and removing the 74 co-located tests
  the audit had frozen.
- Validation: `pnpm check` -> `structure-audit: passed (16 warnings, 40
  baselined)`, every package typechecks, exit 0. `pnpm build` -> exit 0, with
  51 compiled tests all under `dist/tests/` and zero left at the pre-move
  depth. `pnpm -r --filter '!@fluxiq-web-extension/test-runner' test` ->
  exit 0, 97 tests, 0 failures. `pnpm test` -> fails only in `test-runner`,
  19 failures identical in count and name to before the move; 18 are a cwd
  artifact of `pnpm --filter` (from the repository root the same 266 tests
  give 265 pass / 1 fail) and the 19th is a stale config assertion in
  `demo-llm-creation.test.ts`. `node scripts/structure-audit.mjs --rule
  test-placement --json` -> zero findings, down from 13 directories / 74
  files. Baseline diff audited key by key: 14 removed, 1 added, 0 raised;
  the single addition is `directory-files` on the new
  `packages/test-runner/src/tests` (51).
- Outcome: Accepted
- Follow-up: `connection.ts` split; carry `test-runner`'s tests with the
  `demo-` prefix split rather than treating `src/tests` as a separate
  problem.

## Open Questions

- **Should the size check live in `packages/boundary-audit` or as a separate
  script?** Reusing the existing audit package avoids a second tool, but
  couples a generic size rule to a repository-specific boundary tool that
  has no counterpart in Core. Owner: senior supervisor agent.

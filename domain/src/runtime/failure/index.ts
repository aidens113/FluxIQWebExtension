// The web-automation failure taxonomy's producing half: the closed set of
// codes, and the classifier that lands an outcome on one of them. Core owns the
// categories and the record shape; nothing here restates them.
export * from "./classify";
export * from "./codes";
